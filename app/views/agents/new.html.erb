<div class="login-and-creator register">
  <h4>Create a New Council Account</h4>
  <div class="aside-container">
    <%= image_tag  "councils.jpg" %>
  </div>
  <div class="page-form" autocomplete="off">
    <%= my_form_for @agent, url: agents_path(@agent), html: { class: "form new-account", autocomplete: 'false' } do |f| %>
      <h5>New Account Form</h5>
      
      <%= f.label :council_id, 'Council' %>
      <%= f.select :council_id, options_for_select(Council.all.map { |r| [r.name, r.id] }.unshift(['Choose Council', 88888888]), @agent.council_id), {}, { class: 'sex chosen-select' } %>
      <%= f.label :full_names, 'Full names' %>
      <%= f.text_field :full_names %>
      <%= f.label :phone_number, 'Phone number' %>
      <%= f.text_field :phone_number %>
      <%= f.label :email, 'Email' %>
      <%= f.email_field :email %>
      <%= f.label :username, 'Username' %>
      <%= f.text_field :username %>
      <%= f.label :password, 'Password' %>
      <%= f.password_field :password %>
      <%= f.label :password_confirmation, 'Confirmation password' %>
      <%= f.password_field :password_confirmation %>
      <%= f.label :code, 'Council Code' %>
      <%= f.text_field :code %>
      <div class="submitter">
        <button type="submit">Create Account</button>
      </div>
    <% end %>
  </div>
  <script>
    $(".chosen-select").chosen()
  </script>
</div>



