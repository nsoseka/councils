<header class="stat-page">
  <h1 class="stat-page"><%= t('statistics.birth_statistics') %></h1>
  <nav class="stat-nav">
    <ul>
      <li class="<%= @chosen == 'day' ? 'stat-chosen' : '' %>">
        <%= link_to births_this_day_path do %>
          <%= t('statistics.day') %>
        <% end %>
      </li>
      <li class="<%= @chosen == 'week' ? 'stat-chosen' : '' %>">
        <%= link_to births_this_week_path do %>
          <%= t('statistics.week') %>
        <% end %>
      </li>
      <li class="<%= @chosen == 'month' ? 'stat-chosen' : '' %>">
        <%= link_to births_this_month_path do %>
          <%= t('statistics.month') %>
        <% end %>
      </li>
      <li class="<%= @chosen == 'year' ? 'stat-chosen' : '' %>">
        <%= link_to births_this_year_path do %>
          <%= t('statistics.year') %>
        <% end %>
      </li>
    </ul>
  </nav>
</header>

<% if !@stats[:mean].nil? && @stats[:mean] > 0 %>
  <%= render 'charts', { data: @data, stats: @stats } %>
<% else %>
  <h3 class="nothing"><%= t('statistics.no_births') %> #*<%= t('statistics.' + @chosen) %>*</h3>
<% end %>

