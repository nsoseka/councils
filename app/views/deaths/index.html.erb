<h1><%= t('.todays_deaths') %></h1>
<ul class="children">
  <% if @deaths.empty? %>
    <h3><%= t('.no_deaths') %></h3>
  <% else %>
    <%== pagy_nav(@pagy) %>
    <% @deaths.each do|t|%>
      <%= render "death", {death: t} %>
    <% end %>
  <% end %>
</ul>

