<h1><%= t('.todays_deaths') %></h1>
<ul class="items">
  <% if @deceaseds.empty? %>
    <h3><%= t('.no_deaths') %></h3>
  <% else %>
    <%== pagy_nav(@pagy) %>
    <% @deceaseds.each do |deceased|%>
      <%= render 'deceaseds/deceased', { deceased: deceased } %>
    <% end %>
  <% end %>
</ul>
