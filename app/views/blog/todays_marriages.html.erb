<h1><%= t('statistics.todays_marriages') %></h1>
<p class="new-marriages basic-stats"><%= @stats[:sum].to_i %> <%= t('statistics.number_mt') %></p>
<% if !@stats[:mean].nil? && @stats[:mean] > 0 %>
  <%= render 'statistics/charts', { data: @data, stats: @stats } %>
<% else %>
  <h3 class="nothing"><%= t('statistics.no_marriages') %></h3>
<% end %>
